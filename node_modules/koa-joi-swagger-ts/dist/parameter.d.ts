import { ISchema } from './ischema';
import * as joi from 'joi';
export declare const TAG_PARAMETER: unique symbol;
export interface IParameter {
    in: ENUM_PARAM_IN;
    schema: joi.Schema | ISchema;
}
export declare enum ENUM_PARAM_IN {
    query = 0,
    body = 1,
    header = 2,
    path = 3,
    formData = 4
}
export declare function parameter(name: string, schema?: ISchema | joi.Schema, paramIn?: ENUM_PARAM_IN): MethodDecorator;
